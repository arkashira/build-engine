[project]
name = "build-engine-session-dashboard"
version = "0.1.0"
description = "A minimal session management and billing dashboard."
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = ["tests"]
